// kind of ugly to define it like this
// so the ujs and glue stuff works
// window.React = require('react');
// require('react_ujs');
// require('contrib/react_glue');

window.ReactHome = require('react_home');
